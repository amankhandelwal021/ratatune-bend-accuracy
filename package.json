{
  "name": "ratatune-bend-accuracy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ratatune bend accuracy landing page with a live browser microphone prototype.",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir .prerender && node scripts/prerender.mjs",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "package": "npm run build && node scripts/package.mjs",
    "format": "prettier --write src scripts tests vite.config.js playwright.config.js"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@vitejs/plugin-react": "6.1.1",
    "fflate": "0.8.3",
    "prettier": "3.9.6",
    "vite": "8.2.2"
  }
}
